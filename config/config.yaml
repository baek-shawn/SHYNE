# SHYNE 설정 파일 (placeholder)
#
# Step 1에는 아직 이 파일을 읽는 코드가 없다. LLM Provider 분리(Step 3)를 구현할 때
# provider 종류(vllm/openai/anthropic)와 접속 정보, Agent Loop 안전장치
# (max_iterations, command_timeout, max_consecutive_errors) 값을 이 파일로 옮길 예정이다.
# 그 전까지는 실제 키/URL 값을 이 파일에 커밋하지 않는다 — 민감한 값은 .env로 분리한다.
